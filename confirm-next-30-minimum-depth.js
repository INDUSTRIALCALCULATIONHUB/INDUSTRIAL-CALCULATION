const fs = require('fs');
const note = (title) => `<section class="content-depth-section"><h2>Decision Record Check</h2><p>For ${title}, record the actual decision supported, governing condition, source and revision of significant inputs, method used, stated limitations, required approval and post-change verification. This makes the educational guidance useful in practice while keeping it separate from a controlled project calculation or specification.</p><p>Before acting, confirm that the evidence uses the current arrangement, compatible units and reference conditions, a credible governing case, and the right measurement or inspection basis. Where any of these items remains uncertain, collect better information or obtain qualified specialist review instead of treating a preliminary conclusion as final approval.</p></section>`;
const files=fs.readdirSync('engineering',{recursive:true}).filter(f=>f.endsWith('index.html')).map(f=>`engineering/${f.replaceAll('\\','/')}`);let updated=0;
for(const file of files){let html=fs.readFileSync(file,'utf8');if(!html.includes('<!-- next30-')||html.includes('<h2>Decision Record Check</h2>'))continue;const title=(html.match(/<h1[^>]*>([\s\S]*?)<\/h1>/i)?.[1]||'this engineering guide').replace(/<[^>]+>/g,'').replace(/\s+/g,' ').trim();const start=html.indexOf('<!-- next30-'),end=html.indexOf('-end -->',start);if(start<0||end<0)throw new Error(file);const block=html.slice(start,end);const next=block.replace('<h3>Expanded FAQs</h3>',`${note(title)}<h3>Expanded FAQs</h3>`);if(next===block)throw new Error(`FAQ ${file}`);html=html.slice(0,start)+next+html.slice(end);fs.writeFileSync(file,html,'utf8');updated++};if(updated!==30)throw new Error(`Expected 30, got ${updated}`);console.log(`Confirmed depth for ${updated} guides.`);
