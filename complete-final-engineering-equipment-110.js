const fs = require('fs');
const path = require('path');
const root = process.cwd();
const skip = new Set(['.git', 'assets', 'outputs', 'tmp', 'design-prototypes']);
function walk(dir) { return fs.readdirSync(dir, {withFileTypes:true}).flatMap(e => e.isDirectory() ? (skip.has(e.name) ? [] : walk(path.join(dir,e.name))) : (e.isFile() && e.name.endsWith('.html') ? [path.join(dir,e.name)] : [])); }
function title(html) { return (html.match(/<h1\b[^>]*>([\s\S]*?)<\/h1>/i)?.[1] || 'This engineering guide').replace(/<[^>]+>/g,'').replace(/\s+/g,' ').trim(); }
const lifecycle = (pageTitle) => `<section class="content-depth-section"><h2>Lifecycle Evidence, Field Verification and Change Control</h2><p>${pageTitle} should remain connected to current evidence throughout its service life. Material variation, wear, fouling, corrosion, temperature, loading, contamination, control changes, maintenance practice and upstream process variation can change the basis on which equipment or a calculation was originally selected.</p><h3>Maintain a usable evidence set</h3><p>Record whether each important input is measured, calculated, supplier-rated, estimated or assumed. Retain the source, revision, date, units, reference condition, measurement location and expected uncertainty. This prevents a result from being compared with an obsolete data sheet, a different operating case or a measurement taken at another system boundary.</p><p>Use equivalent operating conditions when comparing field trends. Document production load, material or fuel condition, relevant pressure and temperature, equipment configuration, controls, instruments and maintenance state. A plausible trend can be misleading if these conditions are not comparable.</p><h3>Check the actual governing condition</h3><p>Review normal operation as well as start-up, shutdown, low load, maximum duty, dirty condition, maintenance bypass, upset, seasonal effect and credible future modification. The governing case may control capacity, reliability, integrity, emissions, quality, energy, electrical duty, serviceability or safety.</p><p>If reasonable uncertainty changes a decision, improve the evidence through inspection, representative testing, calibrated measurement, supplier confirmation, a controlled trial or specialist analysis. This is more valuable than reporting extra decimal places from an uncertain basis.</p><h3>Turn maintenance into engineering information</h3><p>Inspection findings can reveal local wear, leakage, buildup, cracking, corrosion, misalignment, overheating, abnormal vibration, control instability or loss of access that simple selection methods do not show. Record the location, condition, observed mechanism, action and follow-up result so future decisions use the actual service history.</p><p>Define the early-warning parameters, review trigger, responsible role and escalation path. Repeated alarms, manual intervention, rising energy, pressure loss, reduced capacity, dust release, unstable flow or recurring component damage should be investigated as system evidence, not reset as isolated symptoms.</p><h3>Implement controlled change</h3><p>Before changing material, equipment, layout, settings, controls, operating procedure or maintenance practice, check affected drawings, equipment limits, protective functions, isolation requirements, permits, training, spares and downstream interfaces. A local improvement can move a problem to another part of the system.</p><p>After implementation, compare measured performance with stated acceptance criteria at comparable conditions, update the controlled record and document any remaining limitation. This page is an educational reference; final project, code, safety, environmental, electrical and procurement decisions require qualified review with current site information.</p></section>`;
let updated=0;for(const file of walk(root)){let html=fs.readFileSync(file,'utf8');if(!html.includes('<!-- next110-')||html.includes('Lifecycle Evidence, Field Verification and Change Control'))continue;const start=html.indexOf('<!-- next110-'),end=html.indexOf('-end -->',start);if(start<0||end<0)throw new Error(file);const block=html.slice(start,end);const next=block.replace('<h3>Expanded FAQs</h3>',`${lifecycle(title(html))}<h3>Expanded FAQs</h3>`);if(next===block)throw new Error(`FAQ missing: ${file}`);html=html.slice(0,start)+next+html.slice(end);fs.writeFileSync(file,html,'utf8');updated++};if(updated!==110)throw new Error(`Expected 110, got ${updated}`);console.log(`Completed depth layer for ${updated} guides.`);
